# service-cnpj

microsserviço de validação de cnpj.

## endpoints

- `GET /health`: status do serviço
- `GET /validate?cnpj={cnpj}`: valida via query
- `GET /cnpj/{cnpj}`: valida via path
- `POST /validate`: valida via json

---
projeto desenvolvido por joaopssx - joão pedro de sena santana
